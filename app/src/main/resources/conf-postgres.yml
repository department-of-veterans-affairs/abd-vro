
# useful if we use Postgres instead of H2 in dev
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/example
    username: example_flyway
    password: not-the-password
    hikari:
      jdbc-url: jdbc:postgresql://localhost:5432/example
      username: example_flyway
      password: not-the-password
