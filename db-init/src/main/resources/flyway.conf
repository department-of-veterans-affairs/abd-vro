flyway.url=${FLYWAY_URL}
flyway.user=${FLYWAY_USER}
flyway.password=${FLYWAY_PASSWORD}
flyway.connectRetries=${FLYWAY_CONNECT_RETRIES}
#flyway.initSql=
flyway.defaultSchema=${FLYWAY_DEFAULT_SCHEMA}
flyway.schemas=${FLYWAY_SCHEMA}
flyway.createSchemas=${FLYWAY_CREATE_SCHEMAS}
flyway.table=${FLYWAY_TABLE}
flyway.tablespace=${FLYWAY_TABLESPACE}
flyway.locations=${FLYWAY_LOCATIONS}
flyway.sqlMigrationPrefix=${FLYWAY_SQL_MIGRATION_PREFIX}
flyway.repeatableSqlMigrationPrefix=${FLYWAY_REPEATABLE_SQL_MIGRATION_PREFIX}
flyway.sqlMigrationSeparator=${FLYWAY_SQL_MIGRATION_SEPARATOR}
flyway.sqlMigrationSuffixes=${FLYWAY_SQL_MIGRATION_SUFFIXES}
flyway.encoding=${FLYWAY_ENCODING}
flyway.placeholderReplacement=${FLYWAY_PLACEHOLDER_REPLACEMENT}
#flyway.placeholders.flywayname=example_flyway
flyway.placeholders.username=${FLYWAY_PLACEHOLDERS_USERNAME}
flyway.placeholders.servicename=${FLYWAY_PLACEHOLDERS_SERVICE_NAME}
flyway.placeholders.adminname=${FLYWAY_PLACEHOLDERS_ADMIN_NAME}
flyway.placeholders.dbname=${FLYWAY_PLACEHOLDERS_DB_NAME}
flyway.placeholders.schemaname=${FLYWAY_PLACEHOLDERS_SCHEMA_NAME}
flyway.placeholders.userpassword=${FLYWAY_PLACEHOLDERS_USER_PASSWORD}
flyway.placeholders.servicepassword=${FLYWAY_PLACEHOLDERS_SERVICE_PASSWORD}
flyway.placeholders.adminpassword=${FLYWAY_PLACEHOLDERS_ADMIN_PASSWORD}
flyway.placeholderPrefix=${FLYWAY_PLACEHOLDER_PREFIX}
flyway.placeholderSuffix=${FLYWAY_PLACEHOLDER_SUFFIX}
flyway.skipDefaultCallbacks=${FLYWAY_SKIP_DEFAULT_CALLBACKS}
flyway.target=${FLYWAY_TARGET}
flyway.outOfOrder=${FLYWAY_OUT_OF_ORDER}
flyway.validateOnMigrate=${FLYWAY_VALIDATE_ON_MIGRATE}
flyway.cleanOnValidationError=${FLYWAY_CLEAN_ON_VALIDATION_ERROR}
flyway.mixed=${FLYWAY_MIXED}
flyway.group=${FLYWAY_GROUP}

# No longer supported.
# Instead, use https://flywaydb.org/documentation/configuration/parameters/ignoreMigrationPatterns
# flyway.ignoreMissingMigrations=false
# flyway.ignoreIgnoredMigrations=false
# flyway.ignoreFutureMigrations=false

flyway.cleanDisabled=${FLYWAY_CLEAN_DISABLED}
flyway.baselineOnMigrate=${FLYWAY_BASELINE_ON_MIGRATE}
flyway.installedBy=${FLYWAY_INSTALLED_BY}
