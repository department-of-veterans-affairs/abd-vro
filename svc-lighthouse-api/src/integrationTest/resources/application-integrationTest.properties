# When @ActiveProfiles("integrationTest") is used, application.properties and application-nonprod.properties
# are loaded due to Spring Profiles configured in application.properties, then this file is loaded

spring.rabbitmq.host=localhost
