# Many of these aren't needed since application.properties should already set them to the same values
# TODO: run the tests with `@ActiveProfiles("integrationTest")` and remove unnecessary settings below
#   See application-test.properties for an example.
#   May even be able to use `@ActiveProfiles({"test","integrationTest"})` to remove more settings

abd-data-access.rabbitmq.claim-submit-exchange=claim-submit-exchange
abd-data-access.rabbitmq.claim-submit-queue=claim-submit
abd-data-access.rabbitmq.claim-submit-routing-key=code.#

lh.tokenurl=${LH_TOKEN_URL}
lh.fhirurl=${LH_FHIR_URL}
lh.pemkey=${LH_PRIVATE_KEY}
lh.assertionurl=${LH_ASSERTION_URL}
lh.clientId=${LH_ACCESS_CLIENT_ID}

spring.rabbitmq.host=localhost
