version: '3.9'

networks:
  internal:

#volumes:
#  pgdata:

services:
  rabbitmq1:
    image: rabbitmq:3
    hostname: rabbitmq1
    expose:
      - "5672"
#    volumes:
#      - mqdata:/var/lib/??/data
    networks:
      - internal
    ports:
      - "5672:5672"

  service-python:
    image: va/abd_vro-service-python:latest
# NOTE: These are the defaults from the flyway.conf   uncomment to override, esp for <secret-service-name> passwords
    environment:
      DOCKER_LOGS: 1
      RABBITMQ_PLACEHOLDERS_USERNAME: guest
      RABBITMQ_PLACEHOLDERS_USERPASSWORD: guest
#    build:
#      context: ../
#      dockerfile: ./docker/Dockerfile
#    tmpfs:
#      - /tmp
#      - /app/tmp/pids
    depends_on:
      - rabbitmq1
    ports: 
      - "5000:5000"
    networks:
      - internal
