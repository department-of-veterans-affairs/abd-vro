
global:
  environment: prod

  # endpoint is api if ENV==prod; otherwise dev
  # Used by virtual-service's hosts
  endpoint: api

  imagePrefix: ""

  pgdata:
    storageSize: 5Gi
  tracking:
    storageSize: 2Gi
