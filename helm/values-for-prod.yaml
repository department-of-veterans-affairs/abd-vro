
global:
  # endpoint is api if ENV==prod; otherwise dev
  # Used by virtual-service's hosts
  endpoint: api

  images:
    repo: abd-vro-internal

  pgdata:
    storageSize: 5Gi
  tracking:
    storageSize: 2Gi

rabbitmq-chart:
  image:
    tag: "3"

redis-chart:
  image:
    tag: "7"
