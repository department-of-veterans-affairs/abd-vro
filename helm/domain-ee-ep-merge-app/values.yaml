labels:
  app: domain-ee-ep-merge-app

serviceUriPrefix: employee-experience/ep-merge

replicaCount: 1

# autoscaling:
#   enabled: false
#   minReplicas: 1
#   maxReplicas: 100
#   targetCPUUtilizationPercentage: 80

service:
  eeEpMergeApp:
    sourcePort: 8140
    targetPort: 8140
