apiVersion: v1
kind: Service
metadata:
  # name is used as the hostname of the service
  name: {{ .Values.global.hostnamePrefix }}-rabbitmq
  labels: {{- toYaml .Values.labels | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.global.service.mq.sourcePort }}
      targetPort: {{ .Values.global.service.mq.targetPort }}
      protocol: TCP
      name: http
  selector: {{- toYaml .Values.labels | nindent 4 }}
