# Best practices: https://helm.sh/docs/chart_best_practices/values/

rabbitmq:
  enabled: true
  extraConfiguration: |
    loopback_users = none
redis:
  enabled: true

global:
  # Persistent Volumes
  pv:
    tracking:
      storageSize: 1Gi
