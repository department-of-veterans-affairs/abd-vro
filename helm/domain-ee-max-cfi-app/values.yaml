labels:
  app: domain-ee-max-cfi-app

serviceUriPrefix: employee-experience

replicaCount: 1

# autoscaling:
#   enabled: false
#   minReplicas: 1
#   maxReplicas: 100
#   targetCPUUtilizationPercentage: 80

service:
  eeMaxCfiApp:
    sourcePort: 8130
    targetPort: 8130
