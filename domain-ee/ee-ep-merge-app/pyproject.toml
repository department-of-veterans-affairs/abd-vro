# pyproject.toml
[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-rfesp"
testpaths = [
    "tests"
]
env = [
    "REQUEST_TIMEOUT = 1",
    "REQUEST_RETRIES = 1",
    "RABBITMQ_USERNAME=user",
    "RABBITMQ_PASSWORD=bitnami"
]