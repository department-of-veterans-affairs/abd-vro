name: Deploy to DEV

on:
  push:
    branches: [ develop ]

  # Allow manual triggering
  workflow_dispatch:

jobs:
  deploy-dev-images:
    uses: ./.github/workflows/deploy-unsigned.yml
    with:
      target_env: dev
      image_prefix: dev_
    secrets: inherit
