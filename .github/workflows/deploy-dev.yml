name: Deploy to DEV

on:
  push:
    branches: [ develop ]

  # Allow manual triggering
  workflow_dispatch:

jobs:
  deploy-images:
    uses: ./.github/workflows/deploy-unsigned.yml
    with:
      target_env: dev
    secrets: inherit
