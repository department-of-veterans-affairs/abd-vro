name: Deploy to DEV

on:
  push:
    branches: [ develop ]

  # Allow manual triggering
  workflow_dispatch:

jobs:
  deploy-dev-images:
    uses: ./.github/workflows/deploy-to-env.yml
    with:
      target_env: dev
    secrets: inherit
