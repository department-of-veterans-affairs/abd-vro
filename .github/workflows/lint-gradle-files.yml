name: "Lint build.gradle files"
on:
  push:
    paths:
      - ./**/*.gradle

jobs:
  lint-gradle-files:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: "Lint Gradle"
        run: ./gradlew lintGradle
