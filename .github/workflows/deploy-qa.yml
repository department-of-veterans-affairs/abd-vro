name: Deploy to QA

on:
  push:
    branches: [ qa ]

  # Allow manual triggering
  workflow_dispatch:

env:
  TARGET_ENV: "qa"

jobs:
  deploy-images:
    uses: ./.github/workflows/deploy-unsigned.yml
    with:
      target_env: qa
    secrets: inherit
