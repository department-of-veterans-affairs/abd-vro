name: Trivy scan
# TODO: delete this once we're using SecRel's Modularized Aqua: aqua.yml@v4.0.0
# https://psychic-disco-c1251ea1.pages.github.io/pipeline/release_notes/v4.0.0/#modularized-aqua-feedback-loop

on:
  pull_request:
    branches: "*"

  # When changes are pushed to special branches
  push:
    branches: [main, qa, develop]

jobs:
  trivy-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: "Scan configs"
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'config'
          hide-progress: false
          format: 'sarif'
          output: 'trivy-config-results.sarif'
          # ignore-unfixed: true
          severity: 'CRITICAL,HIGH'
      - name: Upload Trivy config scan results
        uses: github/codeql-action/upload-sarif@v2
        with:
          sarif_file: 'trivy-config-results.sarif'
          category: trivy-config

      - name: "Set up VRO build env"
        uses: ./.github/actions/setup-vro
      - name: "Build jars and Docker images"
        run: ./gradlew assemble

      - name: "Scan rootfs"
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'rootfs'
#          format: 'sarif'
#          output: 'trivy-rootfs-results.sarif'
          severity: 'CRITICAL,HIGH'
# Alerts aren't consistent with SecRel scan alerts,
# so prevent PR check from failing by not uploading results for now.
#      - name: Upload rootfs results to GitHub Security tab
#        uses: github/codeql-action/upload-sarif@v2
#        with:
#          sarif_file: 'trivy-rootfs-results.sarif'
#          category: trivy-rootfs

# Trivy's image scan shows duplicate alerts as the rootfs scan
# Disabling for now since SecRel does image scans
#      - name: "Scan image: app"
#        uses: aquasecurity/trivy-action@master
#        with:
#          scan-type: 'image'
#          image-ref: 'va/abd_vro-app:latest'
#          format: 'sarif'
#          output: 'trivy-image-results.sarif'
#          severity: 'CRITICAL,HIGH'
#      - name: Upload image results to GitHub Security tab
#        uses: github/codeql-action/upload-sarif@v2
#        with:
#          sarif_file: 'trivy-image-results.sarif'
#          category: trivy-image-app
