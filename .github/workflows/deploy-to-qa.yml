name: Deploy to QA

on:
  push:
    branches: [ qa ]

  # Allow manual triggering
  workflow_dispatch:

jobs:
  deploy-qa-images:
    uses: ./.github/workflows/deploy-to-env.yml
    with:
      target_env: qa
      # QA uses typically dev_* images
      image_prefix: dev_
    secrets: inherit
