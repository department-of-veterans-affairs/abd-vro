name: Deploy to QA

on:
  # push:
  #   branches: [ qa ]

  # Allow manual triggering
  workflow_dispatch:

jobs:
  deploy-qa-images:
    uses: ./.github/workflows/publish-maybe-deploy.yml
    with:
      target_env: qa
      deploy: true
    secrets: inherit
