---

languages:
  java:
    version: 17.0
    build-source: false
    build-args:
      # TODO PR #1549: remove this and test to see if it is still needed
      # Snyk does its own build to run SCA, so it needs the token to pull private artifacts
      - -PGITHUB_ACCESS_TOKEN=${{ secrets.ACCESS_TOKEN_GRADLE_BUILD }}
  python:
    version: 3.9
    requirements:
      - ./domain-cc/svc-cc-app/requirements.txt
