---

languages:
  java:
    version: 17.0
    build-source: false
    build-args:
      # TODO: remove?
      # Snyk does its own build to run SCA, so it needs the token to pull private artifacts
      - -PGITHUB_ACCESS_TOKEN=${{ secrets.ACCESS_TOKEN_GRADLE_BUILD }}
  python:
    version: 3.9
    requirements:
      - ./domain-rrd/service-python/pdfgenerator/src/requirements.txt
      - ./domain-rrd/service-python/assessclaimdc6602/src/requirements.txt
      - ./domain-rrd/service-python/assessclaimdc7101/src/requirements.txt
      - ./domain-rrd/service-python/assessclaimdc6510/src/requirements.txt
