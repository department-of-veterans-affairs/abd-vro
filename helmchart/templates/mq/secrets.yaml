apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.rabbitmq.secretName }}
type: kubernetes.io/basic-auth
stringData:
  username: {{ .Values.rabbitmq.usernameValue }}
  password: {{ .Values.rabbitmq.passwordValue }}